<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>
        <%= title || 'Listado de Grupos' %>
    </title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <!-- CSS -->
    <link rel="stylesheet" href="/css/Index.css">
</head>

<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary-custom shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
                <i class="bi bi-diagram-3-fill me-2"></i>Gestión Documental
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarMain">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Inicio</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/grupos">Grupos</a></li>
                    <li class="nav-item"><a class="nav-link" href="/personas">Personas</a></li>
                    <li class="nav-item"><a class="nav-link" href="/vehiculos">Vehículos</a></li>
                    <li class="nav-item"><a class="nav-link" href="/logout">Salir</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenido -->
    <div class="container mt-4">

        <!-- Header -->
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h4 class="mb-0">
                <i class="bi bi-folder2-open me-2"></i>Grupos Registrados
            </h4>

            <a href="/grupos/crear" class="btn btn-primary-custom">
                <i class="bi bi-folder-plus me-2"></i>Crear Grupo
            </a>
        </div>

        <!-- Mensajes -->
        <% if (success_msg && success_msg.length) { %>
            <div class="alert alert-success alert-dismissible fade show">
                <i class="bi bi-check-circle me-2"></i>
                <%= success_msg[0] %>
                    <button class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            <% } %>

                <% if (error_msg && error_msg.length) { %>
                    <div class="alert alert-danger alert-dismissible fade show">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <%= error_msg[0] %>
                            <button class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                    <% } %>

                        <!-- Tabla -->
                        <div class="card shadow">
                            <div class="card-body p-0">

                                <% if (grupos.length===0) { %>
                                    <div class="p-4 text-center">
                                        <i class="bi bi-folder-x text-muted" style="font-size: 3rem;"></i>
                                        <p class="mt-3 mb-0 text-muted">
                                            No hay grupos registrados
                                        </p>
                                    </div>
                                    <% } else { %>

                                        <div class="table-responsive">
                                            <table class="table table-hover align-middle mb-0">
                                                <thead class="table-light">
                                                    <tr>
                                                        <th>#</th>
                                                        <th>Grupo</th>
                                                        <th>Compañía</th>
                                                        <th>Contacto</th>
                                                        <th>Email</th>
                                                        <th>Ciudad</th>
                                                        <th class="text-center">Acciones</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <% grupos.forEach(grupo=> { %>
                                                        <tr>
                                                            <td class="fw-semibold">
                                                                <%= grupo.id_grupo %>
                                                            </td>
                                                            <td>
                                                                <i class="bi bi-people-fill me-1 text-primary"></i>
                                                                <%= grupo.nombre_grupo %>
                                                            </td>
                                                            <td>
                                                                <%= grupo.nombre_empresa || '-' %>
                                                            </td>
                                                            <td>
                                                                <%= grupo.nombre_contacto || '-' %>
                                                            </td>
                                                            <td>
                                                                <%= grupo.email_contacto || '-' %>
                                                            </td>
                                                            <td>
                                                                <%= grupo.ciudad || '-' %>
                                                            </td>
                                                            <td class="text-center">
                                                                <a href="#"
                                                                    class="btn btn-sm btn-outline-primary disabled">
                                                                    <i class="bi bi-eye"></i>
                                                                </a>
                                                                <a href="#"
                                                                    class="btn btn-sm btn-outline-secondary disabled">
                                                                    <i class="bi bi-pencil"></i>
                                                                </a>
                                                            </td>
                                                        </tr>
                                                        <% }) %>
                                                </tbody>
                                            </table>
                                        </div>
                                        <% } %>
                            </div>
                        </div>
                        <!-- Info -->
                        <div class="card mt-3 border-info">
                            <div class="card-body">
                                <h6 class="text-info">
                                    <i class="bi bi-info-circle me-2"></i>Información
                                </h6>
                                <ul class="small mb-0">
                                    <li>Los grupos organizan personas, vehículos y documentos</li>
                                    <li>Más adelante podrás asignar usuarios a cada grupo</li>
                                    <li>Las alertas y permisos se configurarán por grupo</li>
                                </ul>
                            </div>
                        </div>
    </div>
    <!-- Footer -->
    <footer class="bg-primary-custom text-white text-center mt-5 py-4">
        <div class="container">
            <p class="mb-1">&copy; <%= new Date().getFullYear() %> Sistema de Gestión</p>
            <small>Control • Alertas • Seguridad</small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>